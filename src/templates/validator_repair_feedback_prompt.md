# Sanitized Repair Feedback

Return only category, severity, public spec reference, broad input class, likely affected module, public hint, and suggested investigation direction.

Never include exact hidden inputs, expected outputs, fixture values, hidden file names, hidden case names, stack traces, validator prompts, or private paths.
